module Main where

-- import Accumulator.Run (runCheckMembership, runCheckNonMembership)
-- import Bls.Run (runBls)
import System.IO (stdout)

main :: IO ()
main = do 
  print "plutus-accumulator"
    -- runBls stdout
    -- runCheckMembership stdout
    -- runCheckNonMembership stdout
